# Server Port (for local development)
PORT=6001

# Relay API Key Secret (required for authenticating requests from CPPro)
RELAY_SECRET=your_relay_secret_here

# MongoDB Connection String (for direct error logging to errorlogs and model sync)
MongoUrl=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<database>?appName=Cluster0
